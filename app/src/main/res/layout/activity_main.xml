<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <TextView
        android:id="@+id/tv_header"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/app_name"
        android:textColor="@color/color_aurora"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@color/color_header" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_messages"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:clipToPadding="false" />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:gravity="center_vertical"
        android:background="@color/color_bottom_bar">
        <FrameLayout
            android:id="@+id/input_border_container"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:padding="2dp"
            android:background="@drawable/moving_neon_border">
            <EditText
                android:id="@+id/et_input"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_input_inner"
                android:hint="@string/hint_message"
                android:hintTextColor="@color/color_text_secondary"
                android:textColor="@color/color_on_surface"
                android:textSize="14sp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:inputType="textMultiLine"
                android:maxLines="3"
                android:gravity="center_vertical" />
        </FrameLayout>
        <ImageButton
            android:id="@+id/btn_send"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/ripple_send"
            android:src="@drawable/ic_send"
            android:contentDescription="@string/send" />
    </LinearLayout>
</LinearLayout>
